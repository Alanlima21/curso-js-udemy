const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado'; //função

console.log(resultado(7.1));
console.log(resultado(6.3));

const status = nota >= 7 ? 'Aprovado' : 'Reprovado'; // constante 

